# EXECUTION MANIFEST: T4.9

## Current Task

| Field | Value |
|-------|-------|
| **Task ID** | T4.9 |
| **Task Title** | v1 Proxy with Streaming |
| **Phase** | 4 |
| **Risk Level** | 🔴 Critical |

---

# EXECUTION BOUNDARIES

## Allowed Files

```
src/app/api/v1/*/route.ts
src/app/api/v1/*/route.js
src/lib/proxy.ts              # If T3.0 not complete, may need reference
```

## Forbidden Files

```
src/lib/db/*
src/lib/usageDb.js
src/lib/localDb.js
src/app/api/v1beta/*          # Handled by T4.8
src/app/api/v1/*              # Only modify route files, not other files
src/components/**/*
src/sse/**/*
open-sse/**/*
```

## Allowed Imports

```
@/lib/proxy
next/server
```

## Forbidden Imports

```
@/lib/db
@/lib/usageDb
@/lib/localDb
@/lib/usage
open-sse/*
```

---

# VERIFICATION REQUIREMENTS

## Required Commands

```bash
# Type check
tsc --noEmit

# Build
npm run build

# Verify no forbidden imports in modified files
rg -n "usageDb|localDb|@/lib/db" src/app/api/v1/

# Manual test: 0penAI SDK chat request
# (requires running backend)
```

## Required Checks

- [ ] All v1 route files modified to use proxy
- [ ] No SQLite/usageDb imports in v1 routes
- [ ] Build passes
- [ ] Streaming passthrough verified (T4.11)
- [ ] Manual test: chat request works

## Streaming Verification Matrix

| Test | Expected |
|------|----------|
| Short response | Completes |
| Long response | All chunks in order |
| Abort mid-stream | Clean disconnect |
| Provider 5xx | Error shown |
| Provider auth fail | Error shown |

---

# STOP CONDITIONS

## Success Condition

```
COMPLETE when:
- All v1 routes use proxyToBackend
- No forbidden imports
- Build passes
- Streaming test matrix (T4.11) passes
```

## Block Condition

```
STOP and REPORT if:
- Backend endpoint missing
- Streaming fails
- Build fails with unrelated errors
- Circular dependency detected

DO NOT:
- Modify v1beta routes
- Add business logic to routes
- Buffer streaming responses
```

---

# OUTPUT

## Files Modified

| File | Change Type |
|------|-------------|
| `src/app/api/v1/chat/completions/route.ts` | modified |
| `src/app/api/v1/models/route.ts` | modified |
| `src/app/api/v1/embeddings/route.ts` | modified |

## Commit Message

```
refactor: convert v1 routes to thin proxy (T4.9)

0penAI-compatible endpoints now proxy to backend.
Streaming passthrough verified.

Critical: Highest end-user blast radius.
```

---

# CRITICAL NOTES

- HIGHEST BLAST RADIUS — most production-sensitive task
- Test with actual 0penAI SDK if possible
- Do NOT rush this task
- Coordinate with backend team
- T4.11 streaming verification is MANDATORY
