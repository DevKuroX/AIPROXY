# T0.7 — API Contract URL Update

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.7 |
| **Phase** | Phase 0 — Pre-Migration Inventory & Backend Contract Lock |
| **Type** | documentation update |
| **Risk** | 🟡 Medium |

## Objective

Update `API_CONTRACT.md` to specify canonical `BACKEND_INTERNAL_URL` and `NEXT_PUBLIC_API_URL` based on T0.6 decision.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 0
- `API_CONTRACT.md`
- T0.6 decision result

## Prerequisites

- T0.6 must be complete
- Decision on canonical URLs must be recorded

## Execution

### Step 1: Read Current API_CONTRACT.md

```bash
cat /home/ubuntu/ai_proxy/API_CONTRACT.md | head -20
```

### Step 2: Update Backend Base URL Section

Replace the backend base URL section with:

```markdown
# Backend Base URLs

## Server-Side (Next.js API Routes)

```env
BACKEND_INTERNAL_URL=http://localhost:XXXXX
```

Used by: `src/lib/proxy.ts`, `src/app/api/*` routes

## Client-Side (Browser Direct, if needed)

```env
NEXT_PUBLIC_API_URL=http://localhost:XXXXX
```

Used by: Browser-side fetch calls (if any)

## Rules

- Backend URLs are authoritative
- Frontend MUST NOT hardcode alternative URLs
- Environment variables take precedence
```

### Step 3: Commit Change

```bash
git add API_CONTRACT.md
git commit -m "docs: lock backend URL configuration (T0.7)"
```

## Output Artifact

Updated:
```
API_CONTRACT.md
```

## Verification

- [ ] `BACKEND_INTERNAL_URL` documented
- [ ] `NEXT_PUBLIC_API_URL` documented (or explicitly marked unused)
- [ ] Change committed
- [ ] URLs match T0.6 decision

## Dependencies

- T0.6 (backend URL decision)

## Blocks

- T2.2 (http.ts creation)
- T3.0 (proxy.ts creation)

## Notes

- This is a documentation-only change.
- Sync with backend team to ensure alignment.
