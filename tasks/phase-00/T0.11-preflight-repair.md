# T0.11 - Preflight Repair

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.11 |
| **Phase** | Phase 0 - Pre-Migration Inventory & Backend Contract Lock |
| **Type** | planning repair / documentation |
| **Risk** | Red - blocks reliable execution |

## Objective

Repair planning and authority-document drift before continuing frontend migration execution.

This task exists because the migration plan must be internally consistent before any build, route migration, SQLite removal, or autonomous loop work continues.

## Source Documents

- `AGENTS.md`
- `PROJECT_CONSTRAINTS.md`
- `ARCHITECTURE.md`
- `API_CONTRACT.md`
- `FRONTEND_MIGRATION_TASK_INDEX.md`
- `TASK_STATUS.md`
- `tasks/README.md`
- `.archive/codex-idle-safemode/*`

## Problem Statement

Current planning state has several blockers:

1. Backend port references are inconsistent across task files and docs.
2. `TASK_STATUS.md` task totals do not match task files.
3. `T9.4-delete-dual-shell-js.md` exists but is missing from `TASK_STATUS.md`.
4. Several authority documents referenced by `AGENTS.md` are deleted from the working tree but exist in `.archive/codex-idle-safemode/`.
5. The final stance for Next.js API routes needs to be explicit: temporary migration proxy, permanent BFF, or direct browser-to-Go client.

## Execution

### Step 1: Capture Current Drift

Run read-only checks:

```bash
git status --short --branch
rg -n "20128|14322|1432|BACKEND_INTERNAL_URL|NEXT_PUBLIC_API_URL" -g "*.md" -g "!node_modules/**"
rg --files tasks -g "T*.md"
```

Document the exact mismatches in `FRONTEND_REFACTOR_TRACKER.md` or a dedicated inventory note.

### Step 2: Lock Backend Port Across Planning Docs

Use `API_CONTRACT.md` as the source of truth unless a newer backend-owner decision overrides it.

Canonical value:

```text
http://localhost:1432
```

Update task docs and planning docs that still reference legacy ports such as:

- `20128`
- `14322`
- placeholder `XXXXX`

Do not change backend code in this task.

### Step 3: Repair Task Index / Status Drift

Reconcile:

- `tasks/README.md`
- `FRONTEND_MIGRATION_TASK_INDEX.md`
- `TASK_STATUS.md`
- actual `tasks/phase-*/*.md` files

Required fix:

- Add missing `T9.4` entry to `TASK_STATUS.md`, or explicitly mark it skipped with reason.
- Ensure total task counts match actual task files.
- Ensure `Next Tasks` starts with `T0.11` until this task is complete.

### Step 4: Reconcile Authority Documents

`AGENTS.md` references authority documents that are currently deleted in the working tree:

- `AGENT_RULES.md`
- `DISPATCHER_PROMPT.md`
- `EXECUTION_LOOP.md`
- `TASK_EXECUTION_TEMPLATE.md`

Decide one of:

1. Restore them from `.archive/codex-idle-safemode/`.
2. Update `AGENTS.md` and related docs so deleted documents are no longer authoritative.

Do not leave `AGENTS.md` pointing at missing authority files.

### Step 5: Lock Final Next.js API Route Stance

Record the target stance in `ARCHITECTURE.md` and `FRONTEND_REFACTOR_TRACKER.md`:

- Temporary thin proxy only during migration, then remove where possible.
- Permanent thin BFF/proxy layer.
- Direct browser-to-Go API client where CORS/auth allows.

The decision must preserve the core architecture rule: frontend remains pure UI and must not regain business logic.

## Output Artifacts

- Updated planning docs and task status.
- Documented canonical backend port.
- Documented task-count reconciliation.
- Documented authority-doc reconciliation.
- Documented final stance for Next.js API routes.

## Verification

- [ ] `rg -n "20128|14322" -g "*.md" -g "!node_modules/**"` returns no active migration-doc references, or each remaining reference is explicitly labelled legacy/reference-only.
- [ ] `TASK_STATUS.md` includes every task file under `tasks/phase-*`.
- [ ] `tasks/README.md` total matches actual task files.
- [ ] `AGENTS.md` references only existing authority docs, or missing docs are restored.
- [ ] `Next Tasks` in `TASK_STATUS.md` moves to `T1.7` only after this task is complete.

## Dependencies

- T0.10

## Blocks

- T1.7
- T1.8
- T2.1
- Any autonomous loop execution

## Forbidden

- Modifying frontend application code.
- Modifying backend code.
- Running destructive cleanup commands.
- Marking this task done while task counts or authority docs are still inconsistent.

## Notes

This is a planning repair task. Its purpose is to prevent the migration loop from executing against contradictory instructions.
