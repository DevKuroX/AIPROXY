# T0.4 — Filesystem Import Inventory

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.4 |
| **Phase** | Phase 0 — Pre-Migration Inventory & Backend Contract Lock |
| **Type** | read-only inventory |
| **Risk** | 🟢 Low |

## Objective

Produce a complete list of every file that imports filesystem-related modules from `frontend/src`.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 0
- `PROJECT_CONSTRAINTS.md` § CRITICAL ARCHITECTURE RULE
- `ARCHITECTURE.md` § Final Architecture Goal

## Forbidden Patterns

Frontend MUST NOT contain:
- `DATA_DIR` references
- `APPDATA` references
- Direct filesystem access

## Execution

### Step 1: Run Import Search

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "process\.env\.(DATA_DIR\|APPDATA)" src/
```

### Step 2: Capture Output

Save output to:
```
docs/migration/inventory/fs-importers.txt
```

### Step 3: Verify Output

- [ ] Output file exists
- [ ] Contains line-by-line results
- [ ] Empty output is valid (means zero importers)

## Output Artifact

```
docs/migration/inventory/fs-importers.txt
```

Format: One line per match, `file:line:matched_text`

## Verification

```bash
test -f docs/migration/inventory/fs-importers.txt && echo "PASS" || echo "FAIL"
```

## Dependencies

- None

## Blocks

- T8.4 (dataDir.js deletion)

## Notes

- This is read-only. No code changes.
- `dataDir.js` references `APP_NAME = "9router"` and `process.platform`.
- Frontend should NEVER touch filesystem.
