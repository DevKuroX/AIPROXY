# T0.8 — Backend Coverage Verification

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.8 |
| **Phase** | Phase 0 — Pre-Migration Inventory & Backend Contract Lock |
| **Type** | verification |
| **Risk** | 🟠 High |

## Objective

Confirm each Cohort A endpoint exists on the backend. Document any missing endpoints.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 0
- `API_CONTRACT.md`
- T0.5 classification result

## Endpoints to Verify

### Authentication

- `GET /api/auth/me`
- `POST /api/auth/login`

### Admin

- `GET /api/admin/keys`
- `POST /api/admin/keys`
- `GET /api/admin/providers`
- `POST /api/admin/providers`
- `GET /api/admin/nodes`
- `GET /api/admin/proxy-pools`
- `GET /api/admin/combos`
- `GET /api/admin/pricing`
- `GET /api/admin/models`
- `GET /api/admin/media-providers`
- `GET /api/admin/tags`
- `GET /api/admin/settings`
- `GET /api/admin/usage`

### OAuth

- `GET /api/oauth/providers`

### Health

- `GET /health`
- `GET /version`

## Execution

### Step 1: Start Backend (if not running)

```bash
cd /home/ubuntu/ai_proxy/backend
# Start backend on the canonical port from T0.6
```

### Step 2: Test Each Endpoint

```bash
BACKEND_URL="http://localhost:XXXXX"

# Auth
curl -s -o /dev/null -w "%{http_code}" $BACKEND_URL/api/auth/me
curl -s -o /dev/null -w "%{http_code}" -X POST $BACKEND_URL/api/auth/login

# Admin
curl -s -o /dev/null -w "%{http_code}" $BACKEND_URL/api/admin/keys
# ... repeat for each endpoint
```

### Step 3: Record Results

Create:
```
docs/migration/inventory/backend-coverage.md
```

## Output Artifact

```
docs/migration/inventory/backend-coverage.md
```

Format:
```markdown
# Backend Coverage Report

## Verified Endpoints

| Endpoint | Method | Status | Notes |
|----------|--------|--------|-------|
| /api/auth/me | GET | 200 | OK |
| /api/admin/keys | GET | 200 | OK |

## Missing Endpoints

| Endpoint | Frontend Route | Impact |
|----------|----------------|--------|
| /api/admin/foo | api/foo/ | Blocks T3.X |

## Schema Mismatches

| Endpoint | Expected | Actual | Ticket |
|----------|----------|--------|--------|
| ... | ... | ... | ... |
```

## Verification

- [ ] All Cohort A endpoints tested
- [ ] Missing endpoints documented
- [ ] Schema mismatches documented
- [ ] Backend tickets filed for gaps

## Dependencies

- T0.6 (backend URL decision)
- Backend running

## Blocks

- T2.16 (backend coverage gap verification)
- T3.2 through T3.5 (route conversion)

## Notes

- Backend must be running for this task.
- 404 = missing endpoint; 401/403 = exists but needs auth (OK).
- File backend tickets immediately for missing endpoints.
