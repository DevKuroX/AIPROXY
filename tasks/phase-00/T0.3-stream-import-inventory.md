# T0.3 — Streaming Module Import Inventory

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.3 |
| **Phase** | Phase 0 — Pre-Migration Inventory & Backend Contract Lock |
| **Type** | read-only inventory |
| **Risk** | 🟢 Low |

## Objective

Produce a complete list of every file that imports from `open-sse` (the embedded 9router streaming engine).

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 0
- `FRONTEND_REFACTOR_TRACKER.md` § Known Problem Areas
- `ARCHITECTURE.md` § Streaming Architecture

## Critical Context

`frontend/open-sse/` contains:
- Full provider-streaming engine
- Executors, translator, RTK, transformer
- Duplicates backend functionality

This is the **highest-risk architectural violation**.

## Execution

### Step 1: Run Import Search (src)

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "open-sse" src/
```

### Step 2: Run Import Search (open-sse internal)

```bash
rg -n "open-sse" open-sse/
```

### Step 3: Combine and Capture

Merge both outputs into:
```
docs/migration/inventory/stream-importers.txt
```

### Step 4: Verify Output

- [ ] Output file exists
- [ ] Contains both `src/` and `open-sse/` imports
- [ ] Total import count documented

## Output Artifact

```
docs/migration/inventory/stream-importers.txt
```

Format:
```
# External consumers (src/)
<rg output from src/>

# Internal references (open-sse/)
<rg output from open-sse/>

# Summary
Total external consumers: N
Total internal files: M
```

## Verification

```bash
test -f docs/migration/inventory/stream-importers.txt && echo "PASS" || echo "FAIL"
```

## Dependencies

- None

## Blocks

- T7.7 (requires consumer list)
- T7.11 (deletion depends on zero consumers)

## Notes

- This is read-only. No code changes.
- High import count = high P7 effort.
- This inventory determines streaming migration scope.
