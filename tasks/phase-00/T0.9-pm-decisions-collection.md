# T0.9 — PM Decisions Collection (Appendix-A)

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T0.9 |
| **Phase** | Phase 0 — Pre-Migration Inventory & Backend Contract Lock |
| **Type** | decision collection |
| **Risk** | 🔴 Critical |

## Objective

Collect PM decisions on 7 scoping questions that block Phase 3+ execution.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 0
- `FRONTEND_REFACTOR_EXECUTION_PLAN.md` § Appendix-A

## Questions Requiring PM Decision

### 1. Backend Port Configuration

**Question**: What is the canonical backend port?

**Options**:
- A) `:20128` (as in `lib/api.ts`)
- B) `:14322` (as in `STRUCTURE.md`)
- C) `:1432` (as in `API_CONTRACT.md`)
- D) Other: ____

**Default if unanswered**: Use `:20128` (current frontend default)

---

### 2. Token Storage Strategy

**Question**: Keep localStorage or switch to cookies?

**Options**:
- A) Keep localStorage (current implementation)
- B) Switch to httpOnly cookies (backend manages)
- C) Hybrid (localStorage for access token, cookie for refresh)

**Default if unanswered**: Keep localStorage (per planning doc §7.6)

---

### 3. Init/Shutdown Route Scope

**Question**: Are `api/init/` and `api/shutdown/` routes in scope?

**Options**:
- A) Keep (frontend manages app lifecycle)
- B) Delete (backend manages lifecycle)
- C) Proxy to backend endpoints

**Default if unanswered**: Delete (backend manages lifecycle)

---

### 4. Cloud/Tunnel/CLI Scope

**Question**: Are cloud sync, tunnel, and CLI tool routes in scope?

**Options**:
- A) All in scope (migrate as proxies)
- B) Partial scope (specify which)
- C) All out of scope (delete)

**Default if unanswered**: Out of scope (delete routes)

---

### 5. Locale Route Scope

**Question**: Is `api/locale/` a frontend-only route (Next.js i18n)?

**Options**:
- A) Frontend-only (keep as-is)
- B) Backend-driven (proxy)
- C) Delete (use Next.js built-in i18n)

**Default if unanswered**: Frontend-only (keep as-is)

---

### 6. MITM Alias Scope

**Question**: Is MITM aliasing in scope?

**Options**:
- A) In scope (migrate `mitmAliasCache`, etc.)
- B) Out of scope (delete all MITM-related code)

**Default if unanswered**: Out of scope (delete)

---

### 7. Test Script Disposition

**Question**: What to do with ad-hoc test scripts at frontend root?

Files:
- `login-test.js`
- `test-api-me.js`
- `test-dashboard.js`
- `test-detailed.js`
- `serve-static.js`

**Options**:
- A) Relocate to `scripts/` directory
- B) Delete (replace with proper tests)
- C) Convert to vitest/playwright tests

**Default if unanswered**: Relocate to `scripts/`

---

## Execution

### Step 1: Prepare Decision Document

Create:
```
docs/migration/pm-decisions.md
```

### Step 2: Collect Sign-offs

For each question:
1. Present to PM
2. Record decision
3. Record decision maker
4. Record date

### Step 3: Get Final Sign-off

PM signs off on all decisions.

## Output Artifact

```
docs/migration/pm-decisions.md
```

Format:
```markdown
# PM Decisions — Frontend Migration

**Decision Date**: YYYY-MM-DD
**Decision Maker**: [Name]

| # | Question | Decision | Default Used? |
|---|----------|----------|---------------|
| 1 | Backend port | A | No |
| 2 | Token storage | A | Yes |
| ... | ... | ... | ... |

**PM Sign-off**: ________________ Date: ________
```

## Verification

- [ ] All 7 questions answered
- [ ] PM sign-off obtained
- [ ] Document committed to repo

## Dependencies

- None

## Blocks

- T3.x (Cohort A conversion depends on port decision)
- T4.x (Cohort B decisions depend on scope decisions)
- T5.x (Auth depends on token storage decision)
- T6.x (Legacy modules depend on scope decisions)

## Notes

- This is the **most critical blocking task**.
- Cannot proceed with implementation without these decisions.
- Use defaults only as last resort with PM acknowledgment.
