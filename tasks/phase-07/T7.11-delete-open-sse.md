# T7.11 — Delete open-sse Directory

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T7.11 |
| **Phase** | Phase 7 — Streaming Normalization |
| **Type** | delete |
| **Risk** | 🔴 Critical |

## Objective

Delete `frontend/open-sse/` entirely. Single commit.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 7
- `ARCHITECTURE.md` § Streaming Architecture
- T7.10 streaming parity verification

## Prerequisites

- T7.1 through T7.10 complete
- T7.12 verification confirms zero consumers

## Critical Context

`frontend/open-sse/` contains:
- Full 9router streaming engine
- Executors, translator, RTK, transformer
- ~15+ files
- Duplicates backend functionality

This is the **largest architectural violation** in the project.

## Execution

### Step 1: Final Verification

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "open-sse" src/

# MUST return zero matches
```

If any matches found:
- STOP
- Complete T7.7/T7.8 first

### Step 2: Remove Directory

```bash
rm -rf open-sse/
```

### Step 3: Verify Deletion

```bash
test -d open-sse && echo "FAIL: Directory still exists" || echo "PASS: Deleted"
```

### Step 4: Build Verification

```bash
npm run build
```

### Step 5: Smoke Test

- Dashboard loads
- Chat streaming works
- No console errors

### Step 6: Commit

```bash
git add -A
git commit -m "refactor: delete open-sse streaming engine (T7.11)

The streaming engine now lives entirely in the backend.
Frontend only renders normalized SSE chunks.

BREAKING CHANGE: open-sse imports will fail
"
```

## Output Artifact

- `frontend/open-sse/` — DELETED

## Verification

- [ ] Directory deleted
- [ ] `rg` confirms zero references
- [ ] Build passes
- [ ] Smoke test passes
- [ ] Streaming works with new consumer

## Dependencies

- T7.1 through T7.10
- T7.12 (must confirm zero consumers first)

## Blocks

- T7.13, T7.14

## Forbidden

- Leaving stub files behind
- Recreating under different name
- Partial deletion

## Notes

- This is a **single commit**.
- Do NOT batch with other changes.
- Mark as breaking change in commit message.
- Verify backend handles all streaming before deleting.
