# T7.11 — Delete open-sse Directory

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T7.11 |
| **Phase** | Phase 7 — Streaming Normalization |
| **Type** | deletion |
| **Risk** | 🔴 Critical |

## Objective

Delete `frontend/open-sse/` entirely. Single commit.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 7

## Prerequisites

- T7.10 complete (parity tests pass)

## Execution

### Step 1: Verify No Imports

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "open-sse" src/
```

Must return zero matches.

### Step 2: List open-sse Contents

```bash
ls -la open-sse/
find open-sse -type f | wc -l
```

### Step 3: Delete Directory

```bash
rm -rf open-sse/
```

### Step 4: Verify Deletion

```bash
ls -la open-sse 2>/dev/null && echo "FAIL" || echo "PASS"
```

### Step 5: Verify Build

```bash
tsc --noEmit
npm run build
```

### Step 6: Single Commit

```bash
git add -A
git commit -m "refactor: delete open-sse directory (T7.11)"
```

## Output Artifacts

- `frontend/open-sse/` (deleted)

## Verification

- [ ] No open-sse imports in src/
- [ ] Directory deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T7.10

## Blocks

- T7.12

## Forbidden

- Deleting before verifying zero imports
- Leaving stub files behind

## Notes

- Major milestone: streaming fully backend-owned.
