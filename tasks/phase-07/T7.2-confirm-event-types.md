# T7.2 — Confirm Event Types

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T7.2 |
| **Phase** | Phase 7 — Streaming Normalization |
| **Type** | verification |
| **Risk** | 🔴 Critical |

## Objective

Confirm event types: `delta`, `done`, `error`, and optionally `retry`, `usage`. Each must have stable JSON shape. Update `API_CONTRACT.md` streaming section.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 7
- `API_CONTRACT.md`
- T7.1 output: `docs/STREAM_CONTRACT.md`

## Prerequisites

- T7.1 complete

## Execution

### Step 1: Verify Event Types in Backend

```bash
cd /home/ubuntu/ai_proxy/backend
rg -n '"delta"|"done"|"error"|"retry"|"usage"' internal/stream/
```

### Step 2: Confirm Each Event Type

| Event | Backend Has? | JSON Shape Stable? | Status |
|-------|--------------|-------------------|--------|
| delta | [ ] | [ ] | |
| done | [ ] | [ ] | |
| error | [ ] | [ ] | |
| retry | [ ] | [ ] | Optional |
| usage | [ ] | [ ] | Optional |

### Step 3: Update API_CONTRACT.md

Add streaming section to `API_CONTRACT.md`:

```markdown
## Streaming

### Event Types

All streaming endpoints return normalized events.

#### delta
Content chunk.
\`\`\`json
{ "type": "delta", "content": "string" }
\`\`\`

#### done
Stream complete.
\`\`\`json
{ "type": "done" }
\`\`\`

#### error
Error occurred.
\`\`\`json
{ "type": "error", "message": "string" }
\`\`\`

### Transport

- POST `/api/v1/chat`: SSE via ReadableStream
- POST `/api/v1beta/chat`: SSE via ReadableStream
```

### Step 4: Verify Contract Matches Implementation

Compare `API_CONTRACT.md` with backend code to ensure consistency.

## Output Artifacts

- `API_CONTRACT.md` (updated)

## Verification

- [ ] All event types confirmed in backend
- [ ] JSON shapes verified
- [ ] `API_CONTRACT.md` updated with streaming section

## Dependencies

- T7.1

## Blocks

- T7.3

## Forbidden

- Changing backend event format
- Documenting events that don't exist

## Notes

- API_CONTRACT.md must match backend reality.
- This is the contract frontend will depend on.
