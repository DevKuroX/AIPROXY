# T9.1 — Delete layout.js

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T9.1 |
| **Phase** | Phase 9 — Dual App Shell Collapse |
| **Type** | deletion |
| **Risk** | 🟡 Medium |

## Objective

Delete `src/app/layout.js`. TypeScript version `layout.tsx` remains.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 9
- `BUILD_RULES.md`

## Prerequisites

- Phase 8 complete
- T9.0 complete (if it exists, verify layout.tsx is complete)

## Execution

### Step 1: Verify layout.tsx Exists

```bash
cd /home/ubuntu/ai_proxy/frontend
ls -la src/app/layout.tsx
```

Must exist.

### Step 2: Compare layout.js and layout.tsx

```bash
cat src/app/layout.js
cat src/app/layout.tsx
```

Ensure `layout.tsx` has all functionality from `layout.js`.

### Step 3: Find layout.js Importers

```bash
rg -n "layout.js" src/
```

Should be none (Next.js handles routing).

### Step 4: Delete layout.js

```bash
rm -f src/app/layout.js
```

### Step 5: Verify Deletion

```bash
ls -la src/app/layout.js 2>/dev/null && echo "FAIL" || echo "PASS"
```

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- `src/app/layout.js` (deleted)

## Verification

- [ ] layout.tsx exists and is complete
- [ ] layout.js deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- Phase 8

## Blocks

- T9.2

## Forbidden

- Deleting without verifying .tsx equivalent
- Breaking layout functionality

## Notes

- TypeScript is now the only version.
- Ensure no functionality is lost in the transition.
