# T9.2 — Delete page.js

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T9.2 |
| **Phase** | Phase 9 — Dual App Shell Collapse |
| **Type** | deletion |
| **Risk** | 🟡 Medium |

## Objective

Delete `src/app/page.js`. TypeScript version `page.tsx` remains.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 9
- `BUILD_RULES.md`

## Prerequisites

- T9.1 complete (layout.js deleted)

## Execution

### Step 1: Verify page.tsx Exists

```bash
cd /home/ubuntu/ai_proxy/frontend
ls -la src/app/page.tsx
```

### Step 2: Compare page.js and page.tsx

```bash
cat src/app/page.js
cat src/app/page.tsx
```

Ensure `page.tsx` has equivalent functionality.

### Step 3: Find page.js Importers

```bash
rg -n "page.js" src/
```

Should be none (Next.js handles routing).

### Step 4: Delete page.js

```bash
rm -f src/app/page.js
```

### Step 5: Verify Deletion

```bash
ls -la src/app/page.js 2>/dev/null && echo "FAIL" || echo "PASS"
```

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

### Step 7: Test Page Loads

- [ ] Home page loads
- [ ] Dashboard renders
- [ ] No console errors

## Output Artifacts

- `src/app/page.js` (deleted)

## Verification

- [ ] page.tsx exists and is complete
- [ ] page.js deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] Page loads correctly

## Dependencies

- T9.1

## Blocks

- T9.3

## Forbidden

- Deleting without verifying .tsx equivalent
- Breaking page functionality

## Notes

- TypeScript is now the only version.
- Ensure no functionality is lost.
