# T9.6 — Verify TypeScript Config for App

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T9.6 |
| **Phase** | Phase 9 — Dual App Shell Collapse |
| **Type** | verification |
| **Risk** | 🟢 Low |

## Objective

Verify `tsconfig.json` is properly configured for app directory.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 9

## Prerequisites

- T9.5 complete (imports cleaned)

## Execution

### Step 1: Read tsconfig.json

```bash
cd /home/ubuntu/ai_proxy/frontend
cat tsconfig.json
```

### Step 2: Verify Configuration

Key settings:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
```

### Step 3: Verify App Directory Included

```bash
grep -n "app" tsconfig.json
```

### Step 4: Run Type Check

```bash
tsc --noEmit
```

## Output Artifacts

- None (verification only)

## Verification

- [ ] tsconfig.json properly configured
- [ ] `tsc --noEmit` passes
- [ ] No TypeScript errors

## Dependencies

- T9.5

## Blocks

- T9.7

## Forbidden

- Adding loose type settings

## Notes

- TypeScript configuration should be strict.
- This prevents future type issues.
