# T9.3 — Verify Single App Shell

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T9.3 |
| **Phase** | Phase 9 — Dual App Shell Collapse |
| **Type** | verification |
| **Risk** | 🟡 Medium |

## Objective

Verify only `.tsx` versions of `layout` and `page` exist at `src/app/`.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 9
- `BUILD_RULES.md`

## Prerequisites

- T9.2 complete (page.js deleted)

## Execution

### Step 1: List App Shell Files

```bash
cd /home/ubuntu/ai_proxy/frontend
ls -la src/app/layout.*
ls -la src/app/page.*
```

### Step 2: Verify Only TypeScript

Expected output:
```
src/app/layout.tsx
src/app/page.tsx
```

Should NOT see:
- layout.js
- page.js

### Step 3: Search for Any .js Files

```bash
find src/app -maxdepth 1 -name "*.js" -type f
```

Should return no matches.

### Step 4: Verify Build

```bash
tsc --noEmit
npm run build
```

### Step 5: Test App

- [ ] Home page loads
- [ ] Layout renders
- [ ] Navigation works
- [ ] No console errors

## Output Artifacts

- Verification report

## Verification

- [ ] Only `layout.tsx` exists
- [ ] Only `page.tsx` exists
- [ ] No `.js` files in src/app root
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] App loads correctly

## Dependencies

- T9.2

## Blocks

- T9.4

## Forbidden

- Proceeding if .js files remain

## Notes

- This confirms the dual shell is collapsed.
- TypeScript is now the only option.
