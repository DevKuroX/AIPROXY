# T11.4 — CI Architecture Enforcement

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T11.4 |
| **Phase** | Phase 11 — Final Stabilization & Architecture Lock |
| **Type** | create |
| **Risk** | 🟠 High |

## Objective

Add CI step (lint or grep) that fails the build if forbidden patterns appear in `frontend/src`.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 11
- `PROJECT_CONSTRAINTS.md` § CRITICAL ARCHITECTURE RULE

## Forbidden Patterns

```bash
# SQLite
better-sqlite3
sqlite3
from '@/lib/db'
from '@/lib/usageDb'
from '@/lib/localDb'

# Filesystem
import .* from 'fs' (in non-server-route files)

# Legacy
open-sse
```

## Execution

### Step 1: Create CI Workflow

```yaml
# .github/workflows/ci.yml

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  architecture-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Block SQLite imports
        run: |
          cd frontend
          
          # Check for SQLite imports
          SQLITE=$(rg -l "from ['\"]@/lib/(db/|usageDb|localDb|disabledModelsDb|requestDetailsDb)" src/ || true)
          if [ -n "$SQLITE" ]; then
            echo "ERROR: SQLite imports found:"
            echo "$SQLITE"
            exit 1
          fi
          
          # Check for SQLite packages
          SQLITE_PKG=$(rg -n "better-sqlite3|sqlite3" package.json || true)
          if [ -n "$SQLITE_PKG" ]; then
            echo "ERROR: SQLite packages found in package.json"
            exit 1
          fi
          
          echo "PASS: No SQLite violations"
      
      - name: Block open-sse imports
        run: |
          cd frontend
          
          OPEN_SSE=$(rg -l "open-sse" src/ || true)
          if [ -n "$OPEN_SSE" ]; then
            echo "ERROR: open-sse imports found:"
            echo "$OPEN_SSE"
            exit 1
          fi
          
          echo "PASS: No open-sse violations"
      
      - name: Block filesystem in client code
        run: |
          cd frontend
          
          # Allow fs in server routes (src/app/api/*)
          # Block in client code (src/components/*, src/lib/*, etc.)
          
          FS_IMPORTS=$(rg -l "from ['\"]fs['\"]|from ['\"]fs/promises['\"]" src/components src/lib src/store src/shared || true)
          if [ -n "$FS_IMPORTS" ]; then
            echo "ERROR: Filesystem imports in client code:"
            echo "$FS_IMPORTS"
            exit 1
          fi
          
          echo "PASS: No filesystem violations"

  build:
    runs-on: ubuntu-latest
    needs: architecture-check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - run: cd frontend && npm ci
      - run: cd frontend && npm run build
```

### Step 2: Test Locally

```bash
cd /home/ubuntu/ai_proxy/frontend

# Run the same checks
rg -l "from ['\"]@/lib/(db/|usageDb|localDb)" src/
rg -l "open-sse" src/
rg -l "from ['\"]fs['\"]" src/components src/lib src/store src/shared
```

All should return empty.

### Step 3: Push and Verify CI

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add architecture enforcement (T11.4)"
git push
```

Check GitHub Actions for green build.

## Output Artifacts

```
.github/workflows/ci.yml
```

## Verification

- [ ] CI workflow created
- [ ] All forbidden patterns blocked
- [ ] CI passes on current code
- [ ] CI fails when forbidden pattern added (test)

## Dependencies

- Phase 0-10 complete
- All forbidden patterns already removed

## Blocks

- T11.5 (pre-commit hook)

## Notes

- This makes regressions impossible without conscious override.
- Test by temporarily adding a violation and confirming CI fails.
- Remove test violation after verification.
