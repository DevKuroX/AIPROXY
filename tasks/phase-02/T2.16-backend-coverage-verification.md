# T2.16 — Backend Coverage Gap Verification

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T2.16 |
| **Phase** | Phase 2 — SQLite Dependency Isolation |
| **Type** | verification |
| **Risk** | 🔴 Critical |

## Objective

For each client function created in T2.5–T2.13, verify the backend endpoint exists. STOP and file a backend ticket if any function has NO backend equivalent.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 2
- T0.8 backend coverage report
- T2.5 through T2.13 (created clients)

## Prerequisites

- T2.5–T2.13 complete
- Backend running

## Execution

### Step 1: Compile Function-to-Endpoint Mapping

| Client File | Function | Backend Endpoint |
|-------------|----------|------------------|
| usage-api.ts | getUsageHistory | GET /api/admin/usage/history |
| usage-api.ts | getUsageStats | GET /api/admin/usage/stats |
| settings-api.ts | getSettings | GET /api/admin/settings |
| providers-api.ts | getProviderConnections | GET /api/admin/providers |
| ... | ... | ... |

### Step 2: Verify Each Endpoint

For each function:
1. Check T0.8 coverage report
2. If endpoint listed as verified → PASS
3. If endpoint missing → STOP

### Step 3: File Backend Tickets

For any missing endpoint:

```markdown
# Backend Ticket Template

**Title**: Missing endpoint: GET /api/admin/usage/history

**Frontend Function**: getUsageHistory() in usage-api.ts

**Expected Response**:
\`\`\`json
[
  { "date": "2026-05-01", "requests": 100, "tokens": 5000 }
]
\`\`\`

**Blocking**: T3.5 (usage route migration)

**Priority**: High
```

### Step 4: Halt Phase 2 if Critical Gaps

If any endpoint is missing:
- STOP Phase 2
- File ticket
- Wait for backend implementation

## Output Artifacts

```
docs/migration/backend-coverage-gaps.md
```

Format:
```markdown
# Backend Coverage Gaps

## Verified (PASS)

| Function | Endpoint | Status |
|----------|----------|--------|
| getUsageStats | /api/admin/usage/stats | PASS |
| getSettings | /api/admin/settings | PASS |

## Missing (BLOCKING)

| Function | Expected Endpoint | Ticket # |
|----------|-------------------|----------|
| getUsageHistory | /api/admin/usage/history | #123 |

## Decision Required

- [ ] Continue Phase 2 with partial coverage
- [ ] Wait for backend implementation
```

## Verification

- [ ] All T2.5–T2.13 functions mapped
- [ ] Each endpoint verified or ticket filed
- [ ] No critical blocking gaps (or decision to proceed)

## Dependencies

- T2.5 through T2.13
- T0.8
- Backend running

## Blocks

- Phase 3 (cannot migrate routes without backend)

## Notes

- This is a CRITICAL checkpoint.
- Do NOT proceed to Phase 3 with uncovered endpoints.
- If backend cannot implement, mark feature as out-of-scope.
