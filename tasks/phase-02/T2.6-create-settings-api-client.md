# T2.6 — Create Settings API Client

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T2.6 |
| **Phase** | Phase 2 — SQLite Dependency Isolation |
| **Type** | implementation |
| **Risk** | 🟡 Medium |

## Objective

Create `src/lib/settings-api.ts` — covers backend `/api/settings` endpoints to replace localDb settings functions.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 2
- `API_CONTRACT.md` § Settings
- T2.1 catalog: `docs/migration/inventory/required-clients.md`

## Prerequisites

- T2.1 complete (required clients inventory)
- T2.2 complete (HTTP helper exists)

## Execution

### Step 1: Review Backend Contract

From `API_CONTRACT.md`:

```markdown
## GET /api/settings
Response: { "theme": "dark" }

## POST /api/settings
Request: { "theme": "dark" }
```

### Step 2: Review LocalDb Settings Functions

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "settings" src/lib/localDb.js
rg -n "getSettings|saveSettings" src/
```

### Step 3: Create settings-api.ts

```typescript
// src/lib/settings-api.ts
import { authFetch } from './http';

export interface Settings {
  theme: string;
  // Add other settings fields from backend
}

export async function getSettings(): Promise<Settings> {
  return authFetch<Settings>('/api/settings');
}

export async function updateSettings(settings: Partial<Settings>): Promise<void> {
  return authFetch<void>('/api/settings', {
    method: 'POST',
    body: JSON.stringify(settings)
  });
}
```

### Step 4: Match Function Signatures

Ensure the API client matches the signatures from T2.1 catalog:

| LocalDb Function | Settings API Function |
|-----------------|----------------------|
| `getSettings()` | `getSettings()` |
| `saveSettings(settings)` | `updateSettings(settings)` |

### Step 5: Verify

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- `src/lib/settings-api.ts` (created)

## Verification

- [ ] File created at `src/lib/settings-api.ts`
- [ ] Uses `authFetch` from `http.ts`
- [ ] Matches backend API contract
- [ ] Function signatures match T2.1 catalog
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T2.1, T2.2

## Blocks

- T3.x (route migration for settings)

## Forbidden

- Using SQLite or localDb
- Adding functions not in T2.1 catalog
- Changing backend API shapes
- Adding caching layers

## Notes

- Do NOT import this client anywhere yet — that's Phase 3.
- This is preparation only. The client exists but is not used.
- Follow existing patterns from T2.5 (usage-api.ts).
