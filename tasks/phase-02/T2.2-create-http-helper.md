# T2.2 — Create HTTP Helper

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T2.2 |
| **Phase** | Phase 2 — SQLite Dependency Isolation |
| **Type** | create |
| **Risk** | 🟡 Medium |

## Objective

Create `src/lib/http.ts` — a single `authFetch<T>(path, init)` helper for backend communication.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 2
- `API_CONTRACT.md`
- T0.6 backend URL decision

## Prerequisites

- T0.6 complete (backend URL known)
- T0.7 complete (API_CONTRACT.md updated)

## Execution

### Step 1: Create File

```bash
touch /home/ubuntu/ai_proxy/frontend/src/lib/http.ts
```

### Step 2: Implement authFetch

```typescript
// src/lib/http.ts

const BACKEND_URL = process.env.BACKEND_INTERNAL_URL || 'http://localhost:20128';

export interface AuthFetchOptions extends RequestInit {
  token?: string;
}

/**
 * Authenticated fetch helper for backend API calls.
 * 
 * Behavior:
 * - Prepends BACKEND_URL to path
 * - Adds Authorization header if token available
 * - Forwards cookies for SSR
 * - Returns typed response
 */
export async function authFetch<T>(
  path: string,
  options: AuthFetchOptions = {}
): Promise<T> {
  const { token, ...fetchOptions } = options;
  
  const headers = new Headers(fetchOptions.headers);
  
  // Add Authorization if token provided
  if (token) {
    headers.set('Authorization', `Bearer ${token}`);
  }
  
  // Forward cookies for SSR (server-side requests)
  if (typeof window === 'undefined' && fetchOptions.cookies) {
    // Handle in Next.js context via headers
  }
  
  const response = await fetch(`${BACKEND_URL}${path}`, {
    ...fetchOptions,
    headers,
  });
  
  if (!response.ok) {
    const error = await response.json().catch(() => ({ error: 'Unknown error' }));
    throw new Error(error.error || `HTTP ${response.status}`);
  }
  
  return response.json();
}

/**
 * Server-side proxy helper for Next.js API routes.
 * Streams response body for SSE and binary.
 */
export async function proxyToBackend(
  path: string,
  options: RequestInit = {}
): Promise<Response> {
  const response = await fetch(`${BACKEND_URL}${path}`, options);
  return response;
}
```

### Step 3: Export Token Helpers

```typescript
// Token management (localStorage-based per T0.9 decision)
const TOKEN_KEY = 'token';

export function getToken(): string | null {
  if (typeof window === 'undefined') return null;
  return localStorage.getItem(TOKEN_KEY);
}

export function setToken(token: string): void {
  if (typeof window === 'undefined') return;
  localStorage.setItem(TOKEN_KEY, token);
}

export function clearToken(): void {
  if (typeof window === 'undefined') return;
  localStorage.removeItem(TOKEN_KEY);
}
```

### Step 4: Verify

```bash
tsc --noEmit
```

## Output Artifact

```
src/lib/http.ts
```

## Verification

- [ ] File created
- [ ] `authFetch` function implemented
- [ ] `proxyToBackend` function implemented
- [ ] Token helpers implemented
- [ ] `tsc --noEmit` passes

## Dependencies

- T0.6 (backend URL)
- T0.7 (API_CONTRACT.md)

## Blocks

- T2.3, T2.4, T2.5-2.13 (all API client creation)

## Notes

- This becomes the single auth helper for all API clients.
- Token storage strategy follows T0.9 decision.
- Server-side cookie handling may need Next.js-specific implementation.
