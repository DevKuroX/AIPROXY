# T2.3 — Refactor Admin API

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T2.3 |
| **Phase** | Phase 2 — SQLite Dependency Isolation |
| **Type** | refactor |
| **Risk** | 🟡 Medium |

## Objective

Refactor `admin-api.ts` to import `authFetch` from `src/lib/http.ts`. Verify all callers still pass.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 2
- `API_CONTRACT.md`
- `BUILD_RULES.md`

## Prerequisites

- T2.2 complete (HTTP helper exists)

## Execution

### Step 1: Review Current admin-api.ts

```bash
cd /home/ubuntu/ai_proxy/frontend
cat src/lib/admin-api.ts
```

### Step 2: Identify authFetch Pattern

Find the existing auth fetch logic in admin-api.ts:
- Token retrieval
- Header construction
- Error handling

### Step 3: Import from http.ts

```typescript
// Before
// Inline auth fetch logic...

// After
import { authFetch } from './http';
```

### Step 4: Replace Internal authFetch

Replace any inline auth fetch implementation with the imported `authFetch` from `http.ts`.

### Step 5: Update Call Sites

Ensure all functions in admin-api.ts use the imported `authFetch`:

```typescript
export async function getAdminSettings() {
  return authFetch<SettingsResponse>('/api/admin/settings');
}
```

### Step 6: Verify

```bash
tsc --noEmit
npm run build
```

### Step 7: Test Callers

```bash
# Find all admin-api importers
rg -n "from ['\"]@/lib/admin-api" src/
```

Verify each caller still works:
- Settings page
- Admin routes
- Any other consumers

## Output Artifacts

- `src/lib/admin-api.ts` (modified)

## Verification

- [ ] `authFetch` imported from `http.ts`
- [ ] No duplicate auth fetch logic
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] All callers verified

## Dependencies

- T2.2

## Blocks

- T2.4

## Forbidden

- Changing API function signatures
- Changing response shapes
- Adding new functions
- Removing existing functions

## Notes

- This is a pure refactor — no behavior change.
- Keep all existing exports intact.
- If admin-api.ts already uses a clean authFetch pattern, this task may be minimal.
