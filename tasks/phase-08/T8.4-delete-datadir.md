# T8.4 — Delete dataDir.js

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T8.4 |
| **Phase** | Phase 8 — SQLite Engine Removal |
| **Type** | deletion |
| **Risk** | 🟡 Medium |

## Objective

Delete `src/lib/dataDir.js`. Single commit.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 8
- `BUILD_RULES.md`

## Prerequisites

- T8.3 complete (src/lib/db/ deleted)

## Execution

### Step 1: Find dataDir Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "dataDir" src/
rg -n "from ['\"]@/lib/dataDir" src/
```

### Step 2: Verify Zero Consumers

If consumers exist:
- STOP
- Return to previous phases to complete migration

### Step 3: Delete File

```bash
rm -f src/lib/dataDir.js
```

### Step 4: Verify Deletion

```bash
ls -la src/lib/dataDir.js 2>/dev/null && echo "FAIL" || echo "PASS"
```

### Step 5: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- `src/lib/dataDir.js` (deleted)

## Verification

- [ ] Zero dataDir consumers before deletion
- [ ] File deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T8.3

## Blocks

- T8.5

## Forbidden

- Deleting before verifying zero consumers

## Notes

- dataDir was used for filesystem paths in SQLite era.
- No longer needed in thin-client architecture.
