# T8.2 — Delete Shim Files

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T8.2 |
| **Phase** | Phase 8 — SQLite Engine Removal |
| **Type** | deletion |
| **Risk** | 🟠 High |

## Objective

Delete shim files: `src/lib/usageDb.js`, `localDb.js`, `disabledModelsDb.js`, `requestDetailsDb.js`, `mitmAliasCache.js`. Single commit.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 8
- `BUILD_RULES.md`

## Prerequisites

- T8.1 complete (zero shim importers verified)

## Execution

### Step 1: Final Verification

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "usageDb|localDb|disabledModelsDb|requestDetailsDb|mitmAliasCache" src/
```

Must return zero matches.

### Step 2: List Files to Delete

```bash
ls -la src/lib/usageDb.js
ls -la src/lib/localDb.js
ls -la src/lib/disabledModelsDb.js
ls -la src/lib/requestDetailsDb.js
ls -la src/lib/mitmAliasCache.js
```

### Step 3: Delete All Shims

```bash
rm -f src/lib/usageDb.js
rm -f src/lib/localDb.js
rm -f src/lib/disabledModelsDb.js
rm -f src/lib/requestDetailsDb.js
rm -f src/lib/mitmAliasCache.js
```

### Step 4: Verify Deletion

```bash
ls -la src/lib/usageDb.js 2>/dev/null && echo "FAIL" || echo "PASS"
ls -la src/lib/localDb.js 2>/dev/null && echo "FAIL" || echo "PASS"
ls -la src/lib/disabledModelsDb.js 2>/dev/null && echo "FAIL" || echo "PASS"
ls -la src/lib/requestDetailsDb.js 2>/dev/null && echo "FAIL" || echo "PASS"
ls -la src/lib/mitmAliasCache.js 2>/dev/null && echo "FAIL" || echo "PASS"
```

All should output "PASS".

### Step 5: Verify Build

```bash
tsc --noEmit
npm run build
```

### Step 6: Commit

```bash
git add -A
git commit -m "refactor: delete SQLite shim files (T8.2)"
```

## Output Artifacts

- `src/lib/usageDb.js` (deleted)
- `src/lib/localDb.js` (deleted)
- `src/lib/disabledModelsDb.js` (deleted)
- `src/lib/requestDetailsDb.js` (deleted)
- `src/lib/mitmAliasCache.js` (deleted)

## Verification

- [ ] All shim files deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T8.1

## Blocks

- T8.3

## Forbidden

- Deleting before verifying zero importers
- Leaving stub files behind

## Notes

- This is a major milestone in the migration.
- SQLite shims are no longer needed.
