# T8.5 — Remove SQLite from package.json

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T8.5 |
| **Phase** | Phase 8 — SQLite Engine Removal |
| **Type** | cleanup |
| **Risk** | 🟠 High |

## Objective

Remove SQLite driver from `package.json`: `better-sqlite3`, `sqlite3`, any other SQLite-related package. Re-run `npm install`. Commit lockfile change.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 8
- `BUILD_RULES.md`

## Prerequisites

- T8.4 complete (dataDir.js deleted)

## Execution

### Step 1: List SQLite Dependencies

```bash
cd /home/ubuntu/ai_proxy/frontend
grep -i "sqlite" package.json
```

### Step 2: Remove SQLite Packages

```bash
npm uninstall better-sqlite3
npm uninstall sqlite3
# Remove any other SQLite packages found
```

### Step 3: Verify Removal

```bash
grep -i "sqlite" package.json
```

Should return no matches.

### Step 4: Reinstall Dependencies

```bash
npm install
```

### Step 5: Verify Lockfile

```bash
grep -i "sqlite" package-lock.json
```

Should return no matches.

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

### Step 7: Commit

```bash
git add package.json package-lock.json
git commit -m "refactor: remove SQLite dependencies (T8.5)"
```

## Output Artifacts

- `package.json` (modified)
- `package-lock.json` (modified)

## Verification

- [ ] No SQLite in package.json
- [ ] No SQLite in package-lock.json
- [ ] `npm install` succeeds
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T8.4

## Blocks

- T8.6

## Forbidden

- Leaving SQLite in devDependencies
- Skipping lockfile verification

## Notes

- Removing SQLite from package.json is a major milestone.
- This ensures SQLite can never be accidentally re-imported.
