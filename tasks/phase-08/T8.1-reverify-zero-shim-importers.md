# T8.1 — Re-verify Zero Shim Importers

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T8.1 |
| **Phase** | Phase 8 — SQLite Engine Removal |
| **Type** | verification |
| **Risk** | 🔴 Critical |

## Objective

Re-verify zero shim importers before deletion.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 8
- T0.1 output: `docs/migration/inventory/sqlite-importers.txt`

## Prerequisites

- Phase 7 complete
- Phase 6 complete
- Phase 5 complete
- Phase 3 complete

## Execution

### Step 1: Run Import Check

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/(usageDb|localDb|disabledModelsDb|requestDetailsDb|db/|dataDir|mitmAliasCache)" src/
```

### Step 2: Expected Result

**MUST return ZERO matches.**

### Step 3: If Matches Found

If any matches found:

1. STOP immediately
2. Document which files still import shims
3. Return to appropriate phase to complete migration

Example failure:
```
src/app/api/settings/route.ts:15: import { getSettings } from '@/lib/localDb';
```

This indicates T3.4 (settings routes) was not completed.

### Step 4: Check All Shim Modules

```bash
# Check each shim individually
rg -n "usageDb" src/
rg -n "localDb" src/
rg -n "disabledModelsDb" src/
rg -n "requestDetailsDb" src/
rg -n "mitmAliasCache" src/
rg -n "dataDir" src/
rg -n "@/lib/db" src/
```

All must return zero matches.

### Step 5: Document Verification

Create verification report in `docs/migration/sqlite-zero-importers.md`:

```markdown
# SQLite Shim Importer Verification

**Date**: [DATE]
**Status**: ZERO IMPORTERS VERIFIED

## Verification Commands

```bash
rg -n "usageDb" src/          # Zero matches
rg -n "localDb" src/          # Zero matches
rg -n "disabledModelsDb" src/ # Zero matches
rg -n "requestDetailsDb" src/ # Zero matches
rg -n "mitmAliasCache" src/   # Zero matches
rg -n "dataDir" src/          # Zero matches
rg -n "@/lib/db" src/         # Zero matches
```

## Conclusion

Safe to proceed with shim deletion.
```

## Output Artifacts

- `docs/migration/sqlite-zero-importers.md` (verification report)

## Verification

- [ ] `rg` returns zero matches for all shims
- [ ] Verification report created

## Dependencies

- Phases 3, 5, 6, 7 complete

## Blocks

- T8.2 (cannot delete shims until verified)

## Forbidden

- Proceeding if any imports remain
- Skipping this verification

## Notes

- This is the most critical verification in Phase 8.
- Deleting shims while imports exist will break the build.
