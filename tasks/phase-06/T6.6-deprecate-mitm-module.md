# T6.6 — Deprecate MITM Module

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.6 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟠 High |

## Objective

`src/lib/mitmAliasCache.js` + `src/mitm/` — per Appendix-A scope. If MITM out of scope, delete consumers too.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`
- T0.9 PM decisions (Appendix-A)

## Prerequisites

- T6.5 complete
- T0.2 complete
- T0.9 PM decisions available

## Execution

### Step 1: Check Appendix-A

From T0.9 PM decisions:
- Is MITM in scope for AIPROXY?
- If out of scope, delete all MITM-related code.

### Step 2: List MITM Module Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/mitmAliasCache" src/
rg -n "from ['\"].*mitm" src/
rg -n "mitmAliasCache" src/
```

### Step 3: If OUT OF SCOPE (DELETE)

1. Remove all imports
2. Delete files:
```bash
rm -f src/lib/mitmAliasCache.js
rm -rf src/mitm/
```

### Step 4: If IN SCOPE (MIGRATE)

1. Create API client if needed
2. Replace consumers
3. Delete legacy modules

### Step 5: Verify Zero Imports

```bash
rg -n "mitm" src/lib/
rg -n "mitm" src/
```

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/mitmAliasCache.js` (deleted)
- `src/mitm/` (deleted)

## Verification

- [ ] Decision made based on Appendix-A
- [ ] All MITM consumers handled
- [ ] Modules deleted (if out of scope)
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.5, T0.2, T0.9

## Blocks

- T6.7

## Forbidden

- Deleting without checking Appendix-A
- Leaving broken imports

## Notes

- MITM (Man-in-the-Middle) is specialized functionality.
- Likely out of scope for thin-client AIPROXY.
- Also check if tunnel module was deleted in T6.2.
