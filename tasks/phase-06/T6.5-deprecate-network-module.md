# T6.5 — Deprecate Network Module

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.5 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟡 Medium |

## Objective

`src/lib/network/` — same workflow. Network utilities may be replaced or deleted.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`

## Prerequisites

- T6.4 complete
- T0.2 complete

## Execution

### Step 1: List Network Module Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/network" src/
```

### Step 2: Identify Network Functionality

What does the network module do?
- Network status checking?
- Connection management?
- Proxy configuration?

### Step 3: Determine Action

| Condition | Action |
|-----------|--------|
| Backend handles network logic | DELETE module |
| Frontend needs network utilities | Migrate to simple utility |
| Not used | DELETE |

### Step 4: If DELETE

1. Remove all imports
2. Delete module:
```bash
rm -rf src/lib/network/
```

### Step 5: If MIGRATE

1. Create simplified utility if needed
2. Replace consumers
3. Delete legacy module

### Step 6: Verify Zero Imports

```bash
rg -n "from ['\"]@/lib/network" src/
```

### Step 7: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/network/` (deleted)

## Verification

- [ ] All network consumers handled
- [ ] Module deleted or migrated
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.4, T0.2

## Blocks

- T6.6

## Forbidden

- Deleting without understanding functionality
- Breaking network features if needed

## Notes

- Network logic is typically backend-owned in thin-client architecture.
- Evaluate if any frontend network utilities are needed.
