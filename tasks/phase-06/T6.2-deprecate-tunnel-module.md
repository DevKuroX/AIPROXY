# T6.2 — Deprecate Tunnel Module

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.2 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟠 High |

## Objective

`src/lib/tunnel/` — same workflow; if `tunnel/` is out-of-scope per Appendix-A, delete consumers too.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`
- T0.9 PM decisions (Appendix-A)

## Prerequisites

- T6.1 complete
- T0.2 complete
- T0.9 PM decisions available

## Execution

### Step 1: Check Appendix-A

From T0.9 PM decisions:
- Is tunnel/MITM in scope for AIPROXY?
- If out of scope, delete all tunnel-related code.

### Step 2: List Tunnel Module Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/tunnel" src/
rg -n "tunnel" src/
```

### Step 3: Decision Tree

| Condition | Action |
|-----------|--------|
| Tunnel out of scope | DELETE all consumers and module |
| Tunnel in scope, backend has equivalent | Migrate to API client |
| Tunnel in scope, no backend equivalent | KEEP with PM sign-off |

### Step 4: If DELETE

1. Remove all imports
2. Remove tunnel route (if not done in T4.6)
3. Delete module:
```bash
rm -rf src/lib/tunnel/
```

### Step 5: If MIGRATE

1. Create `tunnel-api.ts` if needed
2. Replace all consumers
3. Delete module

### Step 6: Verify Zero Imports

```bash
rg -n "tunnel" src/lib/
```

### Step 7: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/tunnel/` (deleted)

## Verification

- [ ] Decision made based on Appendix-A
- [ ] All tunnel consumers handled
- [ ] `src/lib/tunnel/` deleted (if out of scope)
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.1, T0.2, T0.9

## Blocks

- T6.3

## Forbidden

- Deleting without checking Appendix-A
- Leaving broken imports

## Notes

- Tunnel/MITM is often specialized functionality.
- May be out of scope for thin-client AIPROXY.
