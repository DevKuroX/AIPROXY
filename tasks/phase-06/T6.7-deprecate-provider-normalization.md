# T6.7 — Deprecate Provider Normalization

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.7 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟡 Medium |

## Objective

`src/lib/providerNormalization.js` — backend-replace or delete.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`

## Prerequisites

- T6.6 complete
- T0.2 complete

## Execution

### Step 1: List Provider Normalization Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/providerNormalization" src/
rg -n "providerNormalization" src/
```

### Step 2: Identify Functionality

What does provider normalization do?
- Normalize provider names?
- Transform provider data?
- Handle provider-specific logic?

### Step 3: Check Backend

```bash
cd /home/ubuntu/ai_proxy/backend
rg -n "normalize|provider" internal/services/
```

Backend likely handles this.

### Step 4: Determine Action

| Condition | Action |
|-----------|--------|
| Backend handles normalization | DELETE module |
| Frontend needs normalization | Migrate to API client or simple utility |
| Not used | DELETE |

### Step 5: If DELETE

1. Remove all imports
2. Delete file:
```bash
rm -f src/lib/providerNormalization.js
```

### Step 6: Verify Zero Imports

```bash
rg -n "providerNormalization" src/
```

### Step 7: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/providerNormalization.js` (deleted)

## Verification

- [ ] All consumers handled
- [ ] Module deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.6, T0.2

## Blocks

- T6.8

## Forbidden

- Deleting without understanding functionality

## Notes

- Provider normalization is typically backend-owned.
- Frontend should receive normalized data from backend.
