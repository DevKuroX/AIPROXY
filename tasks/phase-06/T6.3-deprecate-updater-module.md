# T6.3 — Deprecate Updater Module

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.3 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟡 Medium |

## Objective

`src/lib/updater/` + `src/lib/appUpdater.js` — same workflow. App-updater logic moves to backend or is dropped per PM.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`

## Prerequisites

- T6.2 complete
- T0.2 complete

## Execution

### Step 1: List Updater Module Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/updater" src/
rg -n "from ['\"]@/lib/appUpdater" src/
rg -n "updater" src/
```

### Step 2: Identify Updater Functionality

What does the updater do?
- Check for app updates?
- Download updates?
- Apply updates?

### Step 3: Determine Action

| Condition | Action |
|-----------|--------|
| Backend handles updates | DELETE module |
| Frontend must handle updates | KEEP with PM sign-off |
| Hybrid approach | Migrate to API client |

### Step 4: If DELETE

1. Remove all imports
2. Delete files:
```bash
rm -rf src/lib/updater/
rm -f src/lib/appUpdater.js
```

### Step 5: Verify Zero Imports

```bash
rg -n "updater" src/lib/
```

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/updater/` (deleted)
- `src/lib/appUpdater.js` (deleted)

## Verification

- [ ] All updater consumers handled
- [ ] Module deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.2, T0.2

## Blocks

- T6.4

## Forbidden

- Deleting without understanding functionality
- Breaking update mechanism if needed

## Notes

- App updates are typically backend-managed in production.
- Self-updating frontend is unusual for web apps.
