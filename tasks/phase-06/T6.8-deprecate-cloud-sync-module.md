# T6.8 — Deprecate Cloud Sync Module

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T6.8 |
| **Phase** | Phase 6 — Legacy Module Deprecation |
| **Type** | deprecation |
| **Risk** | 🟡 Medium |

## Objective

`src/lib/initCloudSync.js` — almost certainly delete (cloud sync is backend's responsibility).

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 6
- `BUILD_RULES.md`
- T0.2 output: `docs/migration/inventory/legacy-importers.txt`

## Prerequisites

- T6.7 complete
- T0.2 complete

## Execution

### Step 1: List Cloud Sync Consumers

```bash
cd /home/ubuntu/ai_proxy/frontend
rg -n "from ['\"]@/lib/initCloudSync" src/
rg -n "initCloudSync" src/
```

### Step 2: Identify Functionality

What does cloud sync do?
- Sync data to cloud?
- Handle cloud authentication?
- Manage cloud state?

### Step 3: Verify Backend Handles Sync

```bash
cd /home/ubuntu/ai_proxy/backend
rg -n "cloud|sync" internal/services/
```

### Step 4: Delete Module

Cloud sync is backend's responsibility:

```bash
rm -f src/lib/initCloudSync.js
```

Also delete cloud routes if not done in T4.5.

### Step 5: Verify Zero Imports

```bash
rg -n "initCloudSync" src/
```

### Step 6: Verify Build

```bash
tsc --noEmit
npm run build
```

## Output Artifacts

- Consumer files (modified/deleted)
- `src/lib/initCloudSync.js` (deleted)

## Verification

- [ ] All consumers handled
- [ ] Module deleted
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes

## Dependencies

- T6.7, T0.2

## Blocks

- T6.9

## Forbidden

- Deleting without checking consumers
- Breaking cloud features if in scope

## Notes

- Cloud sync is almost always backend-owned.
- Thin client should not manage cloud sync.
