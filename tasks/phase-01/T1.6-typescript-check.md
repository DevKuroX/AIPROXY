# T1.6 — TypeScript Check

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T1.6 |
| **Phase** | Phase 1 — Shared Contracts Stabilization |
| **Type** | verification |
| **Risk** | 🟡 Medium |

## Objective

Run `tsc --noEmit`. Fix only path-related errors. No semantic changes.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 1
- `BUILD_RULES.md`
- `AGENT_RULES.md`

## Prerequisites

- T1.1 complete (constants import inventory)
- T1.2 complete (constants index normalized)
- T1.3 complete (constants source comments)
- T1.4 complete (API contracts interfaces)
- T1.5 complete (migrate duplicate types)

## Execution

### Step 1: Run TypeScript Check

```bash
cd /home/ubuntu/ai_proxy/frontend
tsc --noEmit
```

### Step 2: Analyze Errors

If errors appear:

1. Read ONLY the FIRST error
2. Identify if it's path-related (import path, module resolution)
3. Identify the file and line number

### Step 3: Fix Path Errors Only

For path-related errors:
- Update import paths to use `@/shared/constants` pattern
- Update import paths to use `@/shared/contracts` pattern

**DO NOT:**
- Change type definitions
- Add `as any` or `@ts-ignore`
- Modify function signatures
- Refactor code structure

### Step 4: Re-verify

```bash
tsc --noEmit
```

Repeat until clean.

### Step 5: Report Any Blockers

If non-path errors appear:
- STOP
- Document the error
- Report to user for decision

## Output Artifacts

- No new files
- Modified files (if path fixes required)

## Verification

- [ ] `tsc --noEmit` exits with code 0
- [ ] No `as any` or `@ts-ignore` added
- [ ] No semantic changes made

## Dependencies

- T1.1, T1.2, T1.3, T1.4, T1.5

## Blocks

- T1.7

## Forbidden

- Type suppression (`as any`, `@ts-ignore`, `@ts-expect-error`)
- Changing type shapes
- Modifying function signatures
- Refactoring code structure

## Notes

- Follow `BUILD_RULES.md` strictly
- Fix ONE error at a time
- Re-run after each fix
- If stuck, report blocker immediately
