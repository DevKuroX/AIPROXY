# T1.2 — Constants Index Normalization

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T1.2 |
| **Phase** | Phase 1 — Shared Contracts Stabilization |
| **Type** | refactor |
| **Risk** | 🟡 Medium |

## Objective

Normalize all `src/shared/constants/` imports to use a single index pattern. Every consumer imports from `@/shared/constants` only (no deep paths).

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 1
- T1.1 inventory results

## Prerequisites

- T1.1 complete (import inventory)

## Execution

### Step 1: Create/Update Index File

```typescript
// src/shared/constants/index.ts

// Re-export all constants from a single entry point
export * from './providers';
export * from './errorCodes';
export * from './models';
// ... etc.
```

### Step 2: Update All Deep Imports

For each file in T1.1 inventory with deep imports:

```typescript
// Before
import { PROVIDERS } from '@/shared/constants/providers';

// After
import { PROVIDERS } from '@/shared/constants';
```

### Step 3: Verify No Deep Imports Remain

```bash
rg -n "from ['\"]@/shared/constants/" src/
# Should return zero matches
```

### Step 4: Build Verification

```bash
cd /home/ubuntu/ai_proxy/frontend
npm run build
```

## Output Artifacts

- `src/shared/constants/index.ts` (created or updated)
- All files with deep imports (modified)

## Verification

- [ ] `rg` returns zero deep imports
- [ ] `tsc --noEmit` passes
- [ ] `next build` passes
- [ ] No behavior change (smoke test)

## Dependencies

- T1.1

## Blocks

- T1.3

## Forbidden

- Adding new constants
- Renaming existing constants
- Changing exported names

## Notes

- Use search-and-replace or AST-based tool for bulk updates.
- Preserve all exported names exactly.
