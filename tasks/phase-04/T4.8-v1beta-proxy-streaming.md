# T4.8 — v1beta Proxy with Streaming

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T4.8 |
| **Phase** | Phase 4 — Cohort B Route Audit & Proxy |
| **Type** | refactor |
| **Risk** | 🔴 Critical |

## Objective

Convert `api/v1beta/*` routes to thin proxies with streaming passthrough. Gemini-compatible endpoints.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 4
- T0.5 route classification
- T3.0 proxy helper

## Prerequisites

- T3.0 complete
- Backend Gemini-compatible endpoints verified
- T0.9 PM decision on scope

## Routes to Convert

```
src/app/api/v1beta/*/route.{ts,js}
```

## Backend Targets

| Frontend Route | Backend Target |
|----------------|----------------|
| /api/v1beta/chat/completions | /v1beta/chat/completions |
| /api/v1beta/models | /v1beta/models |

## Execution

### Step 1: Audit Current Routes

```bash
find src/app/api/v1beta -name "route.ts" -o -name "route.js"
cat src/app/api/v1beta/*/route.ts
```

### Step 2: Identify All Endpoints

List every endpoint under v1beta.

### Step 3: Verify Backend Endpoints Exist

```bash
curl -s http://localhost:20128/v1beta/models
```

### Step 4: Implement Proxy

```typescript
// src/app/api/v1beta/chat/completions/route.ts

import { NextRequest } from 'next/server';
import { proxyToBackend } from '@/lib/proxy';

export async function POST(req: NextRequest) {
  return proxyToBackend(req, '/v1beta/chat/completions');
}

export async function GET(req: NextRequest) {
  return proxyToBackend(req, '/v1beta/models');
}
```

### Step 5: Streaming Verification

For streaming endpoints:
- Verify `ReadableStream` is piped, NOT buffered
- Verify chunk ordering preserved
- Test abort mid-stream

## Output Artifacts

- `src/app/api/v1beta/*/route.ts` (modified)
- `fixtures/after/T4.8-*.har`

## Verification

- [ ] All v1beta routes proxied
- [ ] Streaming passthrough verified
- [ ] HAR diff clean
- [ ] Manual test: Gemini chat request works
- [ ] `tsc --noEmit` passes

## Dependencies

- T3.0
- T4.10 (HAR validation)
- T4.11 (streaming verification)

## Blocks

- P7 (streaming normalization)

## Notes

- **HIGHEST RISK** — Gemini-compatible, production traffic likely.
- Streaming must work perfectly.
- Test against actual Gemini client if possible.
- Coordinate with backend team.
