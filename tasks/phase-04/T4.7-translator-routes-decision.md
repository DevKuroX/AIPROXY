# T4.7 — Translator Routes Decision + Execution

## Task Identity

| Field | Value |
|-------|-------|
| **Task ID** | T4.7 |
| **Phase** | Phase 4 — Cohort B Route Audit & Proxy |
| **Type** | migration |
| **Risk** | 🟠 High |

## Objective

Decide and execute action for `api/translator/*` routes. DELETE — backend has `internal/translator/`.

## Source Documents

- `FRONTEND_MIGRATION_TASK_INDEX.md` § Phase 4
- `BUILD_RULES.md`

## Prerequisites

- T4.6 complete

## Execution

### Step 1: Identify Translator Routes

```bash
cd /home/ubuntu/ai_proxy/frontend
find src/app/api/translator -name "route.*" -type f
```

### Step 2: Read Current Implementation

```bash
cat src/app/api/translator/route.ts
```

### Step 3: Verify Backend Has Translator

```bash
cd /home/ubuntu/ai_proxy/backend
ls -la internal/translator/
rg -n "translator" internal/api/
```

Backend has `internal/translator/` — frontend translator routes can be deleted.

### Step 4: Find Consumers

```bash
rg -n "api/translator" src/
```

### Step 5: Delete Translator Routes

```bash
rm -rf src/app/api/translator/
```

### Step 6: Verify No Broken Imports

```bash
tsc --noEmit
```

### Step 7: Verify Build

```bash
npm run build
```

## Output Artifacts

- `src/app/api/translator/` (deleted)
- Decision documented in tracker

## Verification

- [ ] Translator routes deleted
- [ ] Backend has translator functionality
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] No broken imports

## Dependencies

- T4.6

## Blocks

- T4.8

## Forbidden

- Keeping translator routes if backend has equivalent
- Breaking API compatibility without migration path

## Notes

- Translator logic is now fully backend-owned.
- Frontend should use backend translator API if needed.
- This is a clear DELETE case per task spec.
